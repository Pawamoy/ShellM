stages:
  - quality

style:
  image: shellm/ci:latest
  stage: quality
  script:
    - make check-style

documentation:
  image: shellm/ci:latest
  stage: quality
  before_script:
    - basher install gitlab.com/shellm/doc
  script:
    - make check-documentation

compatibility:
  image: shellm/ci:latest
  stage: quality
  script:
    - make check-compatibility
