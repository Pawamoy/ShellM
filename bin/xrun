#!/bin/bash
## \usage xrun script args
## \brief Load shellm then run a script.
dir="$(dirname "$0")"
dir="$(cd "$dir"/..; pwd)"
export shellm="$dir"
. "${shellm}/usr/shellmrc"

script=$1; shift
. "${script}" "$@"
